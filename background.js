// message passing

// var title, other;
// chrome.runtime.onMessage.addListener(
//   function(request, sender, sendResponse) {
//       dom = 'dasdf'
//       other = 'asdfasdfasdf';
//       if (request.msg === 'fill') {
//         sendResponse([dom, other]);
//       }
//     }
// );
